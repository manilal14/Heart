<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manilal.heart">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PatientLogin"
            android:label="Login as Patient" />
        <activity
            android:name=".PatientRegister"
            android:label="Resister as Patient" />
        <activity
            android:name=".PatientsLoginSuccess"
            android:label="Login Success" />
        <activity
            android:name=".DoctorLogin"
            android:label="Login as Doctor" />
        <activity
            android:name=".GetPasswordForDoctor"
            android:label="Get Password" />
        <activity android:name=".DoctorsLoginSuccess"></activity>
    </application>

</manifest>